---
name: Navbar path
description: The Navbar component lives in src/components/, not src/pages/.
---

The Navbar for the electrical-installers artifact is at:
`artifacts/electrical-installers/src/components/Navbar.tsx`

**Why:** It's a shared UI component, not a page — so it lives in `components/`, following the project's layout convention.
